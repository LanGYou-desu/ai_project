# 🏝 箱庭小镇 · Hakoniwa Town

一个用 **Three.js** 打造的精致 3D 箱庭（微型世界）小镇：漂浮在云海之上的岛屿，
高低错落的台地、丘陵、湖河与海角，白天阳光明媚，夜晚灯火辉煌、烟花绽放。

## ✨ 功能特性

### 昼夜循环
- 太阳/月亮沿天空轨道运行，天空渐变、霞光、星空自动切换
- 晨昏时天空染上橙金色，夜晚升起星星与月亮
- 入夜后：窗户亮灯、路灯点亮、招牌发光、灯塔光束旋转、摩天轮彩灯闪烁、萤火虫飞舞、湖上飘起水灯
- 夜晚随机绽放**烟花**
- 右上角实时时钟 + 时段名称（清晨/正午/黄昏/夜晚…），钟楼指针与真实小镇时间同步

### 高低错落的地形（拒绝“小方块堆砌”）
- 中央台地小镇 + 神社丘陵 + 城堡山 + 林丘草丘 + 海角灯塔
- 湖泊与河流蜿蜒入海，溪流从台地边缘流入湖中（动态水带 + 水雾 + 水花）
- 石路顺应地形蜿蜒起伏，通往神社、城堡、港口都有台阶

### 丰富场景
- 广场喷泉（粒子水柱）、三间发光招牌店铺（茶屋/花屋/面包房）、钟楼、民居群
- 日式神社 + 鸟居 + 石灯笼 + 樱花树；精工天守阁（宽檐四坡顶 + 上层塔楼 + 角楼垛口）+ 五重塔
- 旋转风车、摩天轮（彩灯 + 直立吊舱）、灯塔、港口码头与停泊帆船
- 数百株花草（实例化渲染）、灌木、岩石、松树/阔叶树/樱花树
- **程序化纹理**：屋瓦、灰泥墙面、石砌、木纹、鹅卵石路面、草地颗粒——全部由代码生成
- 木栅栏（栈道边/观景台）、窗棂百叶、檐口墙角饰线等细节

### 载具与动态元素
- 🚗 彩色小车队沿石路往返行驶（含货车）：车轮滚动、车身贴合坡地、夜间车灯/尾灯亮起
- ✈️ 空中飞艇绕岛巡航：吊舱亮灯、螺旋桨旋转、随风轻摆
- 飘移的云、绕岛飞行的鸟群（扇翅）、沿河往返的小船、散步的村民
- 炊烟袅袅、旗帜飘动、树木摇曳、喷泉水柱、湖上水灯漂移
- 摩天轮（吊舱始终直立）/ 风车 / 灯塔光束持续运转

## 🚀 运行方式

**方式一（最简单）**：直接双击打开 `index.html`（已内置本地 three.js，无需联网）。

**方式二（推荐，本地服务器）**：

```bash
cd 3d-hakoniwa-town
python -m http.server 8080
# 浏览器打开 http://localhost:8080
```

Windows 也可双击 `run.bat` 一键启动。

## 🎮 操作说明

| 操作 | 效果 |
| --- | --- |
| 左键拖动 | 旋转视角 |
| 滚轮 | 缩放 |
| 右键拖动 | 平移 |
| 空格 | 暂停/继续时间 |
| 时间滑杆 | 手动切换时刻 |
| 速度按钮 | 0.5× / 1× / 3× / 10× |
| 清晨/正午/黄昏/夜晚 | 一键跳到对应时段 |
| 🔄 自动旋转 | 相机自动环游 |
| ✨ 动态元素 | 开关云/鸟/船/行人等 |
| 🎆 夜晚烟花 | 开关烟花 |
| 📷 截图 | 保存当前画面为 PNG |
| ⛶ 全屏 | 全屏模式 |

## 📁 项目结构

```
3d-hakoniwa-town/
├── index.html          # 页面入口
├── style.css           # UI 样式
├── vendor/three.min.js # 本地化的 three.js r158
├── js/
│   ├── util.js         # 随机 / 噪声 / 程序化纹理 / 材质缓存 / 地基采样 / 接触阴影
│   ├── terrain.js      # 地形高度函数、海/湖/河/溪流
│   ├── town.js         # 建筑、道路、神社、城堡、植被布局
│   ├── vehicles.js     # 汽车车队、飞艇
│   ├── dynamics.js     # 昼夜循环、天空、云鸟、烟花、行人等
│   └── main.js         # 渲染器、相机控制、UI、主循环
├── run.bat             # Windows 一键启动
└── mascot.svg         # 标题卡吉祥物（SVG，无外部依赖）
```

## 🛠 自定义指南

- **调整地形**：编辑 `js/terrain.js` 中 `H.groundHeight`（高原、山丘、湖河都由函数叠加生成）
- **增减建筑**：编辑 `js/town.js` 的 `H.buildTown`，按 `house(x, z, 朝向, 参数)` 等方式添加
- **昼夜速度**：编辑 `js/dynamics.js` 中 `H.env.speed` 或直接在 UI 调整
- **白天时长**：`js/dynamics.js` 中 `env.t + dt * env.speed / 60` 的 60 秒 = 一天

## 🧰 技术栈

- Three.js r158（本地 vendor，支持离线 file:// 打开）
- 纯原生 JS + Canvas 贴图（无外部资源依赖）
- WebGL 着色器：天空渐变、动态水面、瀑布水幕
- 实例化渲染（花草、岩石）保证性能
